import { expect } from 'chai'


describe("Index tests", async () => {

    beforeEach(() => {

    });

    afterEach(() => {
    });


    it("Can calculate 1 + 1", async () => {
        expect(1 === 1)
    });

});